/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package healthcare.Department;

/**
 *
 * @author Muddassar
 */
public class DepartmentFactory {
    
    /*public Department createOrganization(HospitalDepartment.Type type){
        Department organization = null;
        if (type.getValue().equals(DepartmentType.Admin.getValue())){
            organization = new AdministratorDepartment();
            
        }
        else if (type.getValue().equals(HospitalDepartment.Type.Doctor.getValue())){
            organization = new DoctorDepartment();
            
        }
        else if (type.getValue().equals(HospitalDepartment.Type.Lab.getValue())){
            organization = new LabDepartment();
            
        }
        else if (type.getValue().equals(HospitalDepartment.Type.Nurse.getValue())){
            organization = new NurseDepartment();
            
        }
        else if (type.getValue().equals(HospitalDepartment.Type.Patient.getValue())){
            organization = new PatientDepartment();
            
        }
        else if (type.getValue().equals(HospitalDepartment.Type.Pharmacy.getValue())){
            organization = new PharmacyDepartment();
            
        }
        return organization;
    }*/
    
}
